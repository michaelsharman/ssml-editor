{
    "name": "ssml-editor",
    "version": "0.1.1",
    "description": "An SSML editor that accepts plain text and allows you to mark up with SSML.",
    "main": "index.js",
    "scripts": {
        "dev": "npm install && webpack watch --mode development --devtool eval-source-map",
        "prod": "npm install && webpack --mode production",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [
        "ssml"
    ],
    "author": "",
    "license": "MIT",
    "dependencies": {
        "buffer": "^6.0.3",
        "quill": "^2.0.0-rc.2",
        "ssml-check": "github:michaelsharman/ssml-check",
        "stream-browserify": "^3.0.0",
        "string_decoder": "^1.3.0"
    },
    "devDependencies": {
        "css-loader": "^6.10.0",
        "style-loader": "^3.3.4",
        "webpack": "^5.88.2",
        "webpack-cli": "^5.1.4"
    }
}
